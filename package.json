{
  "name": "@azyobuzin-blog/main",
  "private": true,
  "version": "0.2.0",
  "dependencies": {
    "highlight.js": "^11.2.0",
    "milligram": "^1.4.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "parcel": "^2.0.0-rc.0",
    "prettier": "^2.3.2",
    "prettier-plugin-md-nocjsp": "^1.1.1",
    "serve": "^12.0.0"
  },
  "scripts": {
    "build": "npm start -w generator; parcel build --dist-dir public styles/global.scss",
    "serve": "serve public",
    "fix": "npm run fix --workspaces; prettier --ignore-path .gitignore --write ."
  },
  "workspaces": [
    "generator"
  ]
}
